# Core Django Framework
django

# Production Server & Static Files
gunicorn
whitenoise

# Database URL Parsing & Connection
dj_database_url
psycopg2-binary

# Environment Variables
python-decouple

# Google AI (Gemini)
google-generativeai
googleapis-common-protos
google-api-core >= 1.34.1 # Specify minimum version often needed
google-auth >= 2.15.0 # Specify minimum version often needed
protobuf >= 3.19.5 # Specify minimum version often needed
requests >= 2.28 # Specify minimum version often needed

# Other core dependencies (likely needed by Django/others)
sqlparse >= 0.3.1
asgiref >= 3.5.2
typing_extensions # Often needed by modern libraries
